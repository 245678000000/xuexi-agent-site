{
  "name": "xuexi-agent-site",
  "version": "1.0.0",
  "private": true,
  "description": "Agent 学习站 — Obsidian vault → static site",
  "scripts": {
    "build": "pip install -r requirements.txt && python3 build.py",
    "build:local": "python3 build.py",
    "clean": "rm -rf dist"
  },
  "engines": {
    "node": ">=18"
  }
}
